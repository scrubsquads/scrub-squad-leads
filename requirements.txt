# Scrub Squad Lead Generator — Python dependencies
# Versions pinned for reproducibility in GitHub Actions.
# If a version is unavailable, update to latest: pip install --upgrade <package>
outscraper==6.0.1
gspread==6.1.2
google-auth==2.35.0
google-api-python-client==2.151.0
PyYAML==6.0.2
requests==2.32.3
